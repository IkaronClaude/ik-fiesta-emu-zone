<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- ⭐ NO MoonSharp, and no simulation types. This assembly is the 1:1 Zone.exe port and nothing else;
       the bot harness that hosts Lua lives in Fiesta.Emu.Zone.Harness and depends on THIS, never the
       reverse. If a PackageReference to MoonSharp ever reappears here, the boundary has leaked. -->
</Project>
